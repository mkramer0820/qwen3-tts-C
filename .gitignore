*.o
*.wav
!samples/*.wav
!samples/voice_clone_refs/**
!samples/emotion_examples/**
!samples/emotion_dyads/**
!tests/golden/*.wav
*.py
!tests/*.py
qwen_tts
qwen_tts_py/
venv/
venv312/
.venv/
# tools/para (plan_v4 E1): track the tools, NEVER the heavy venv (~3GB). See tools/para/README.md
tools/para/.venv/
!tools/para/*.py
# tools/quant (E7): fake-quant harness for offline quant-format quality gates
!tools/quant/*.py
# NB: no trailing slash — these may be symlinks to the NAS copy, not real dirs
qwen3-tts-0.6b
qwen3-tts-1.7b
qwen3-tts-0.6b-base
qwen3-tts-1.7b-base
qwen3-tts-voice-design
__pycache__/
.DS_Store
TODO.md
weights.bin
bench_scalar
bench_simd
qvoices/
.claude/
server_stderr.log
qwen_tts_cpbench
qwen_tts_decoder_tool
voices/
*.qvoice

# local bench scaffolds / archives (not part of the repo)
ab_*.sh
*.zip

# Local-only infra/dev scripts (private LAN, never commit)
scripts/

# expressivity artifacts (host on GH release/HF, do not commit big binaries)
qwen3-tts-1.7b-expr
# The big .expr weight-deltas are hosted on HF (gabrione/qwen3-tts-italian-expr) and fetched
# on demand by download_assets.sh (sha256-verified against presets/expr/MANIFEST.md, which IS
# tracked). HF is the durable backup now, so we no longer track any .expr in git — this also
# closes the loss-risk behind the 2026-06-15 regression (validated .expr lost with no backup).
presets/expr/*.expr
!training/expressivity-lora/*.py
# Local-only GPU-box trainer/sync scripts (private LAN) — must come AFTER the *.py
# re-include above so they stay ignored (they are pulled to the box, not for the public repo).
training/expressivity-lora/gpu_*
training/expressivity-lora/sync_*to_gpu.sh
# local training data (prep outputs: wav24k + manifests + codes) — never commit
training/expressivity-lora/data*/

# Internal planning / analysis / process docs — LOCAL-ONLY, never commit to the public repo.
# (Cleanup of already-tracked extras like PLAN.md = a later TODO, not today.)
docs/PROJECT-COMPASS.md
plan_emo_v*.md
plan_v*.md
plan_*.md
analisi_*.md
audit_*.md
docs/paralinguistics-*.md
samples/tests/

# Metal/CPU bench outputs (bench_m2.sh)
bench_out/

# Compiler-generated dependency files (Makefile -MMD -MP)
*.d
tests/test_ingot_parity
